app = 'phantom-tiers-bot'
primary_region = 'ord'

[build]
  dockerfile = 'Dockerfile'

# No HTTP service — this is a Discord bot (outbound WebSocket only)

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
